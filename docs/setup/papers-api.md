# Papers API

## Endpoints

- `GET /papers`
- `GET /papers/{id}`
- `PATCH /papers/{id}/status`

## Поддерживаемые фильтры

- `source`
- `category`
- `published_from`
- `published_to`
- `status`
- `min_score`
- `include_scores`

## Пагинация

Используются параметры `limit` и `offset`.

