# Phase 04 Decision

## Решения

- UUID вместо integer IDs
- JSON metadata сохраняется
- схема сразу содержит forward-compatible таблицы следующих фаз
- `paper_status` и `job_status` фиксированы enum-ами

