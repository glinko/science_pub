# Phase 06 Decision

## API

Для milestone 1 достаточно:

- `GET /papers`
- `GET /papers/{id}`
- `PATCH /papers/{id}/status`

## Почему

Этого хватает для ручного просмотра, фильтрации и первичного review без отдельного UI.

