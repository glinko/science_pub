# Phase 05 Decision

## Источник

Первый collector работает только с `arXiv`.

## Правила

- default categories берутся из исходного PDF;
- собираются статьи за последние 24 часа;
- PDF не скачивается, хранится только ссылка;
- дубликаты режутся по `(source, source_id)`.

